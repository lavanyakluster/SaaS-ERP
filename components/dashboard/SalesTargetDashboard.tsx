'use client';

/**
 * Sales Target Dashboard Wrapper Component
 * Exports the sales target dashboard as default for lazy loading
 */

import { SalesTargetDashboard } from '@/components/sales-target/SalesTargetDashboard';

export { SalesTargetDashboard };
export default SalesTargetDashboard;